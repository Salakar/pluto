name: pluto_manifest
description: Pluto app manifest schema and YAML codec.
version: 0.1.0
environment:
  sdk: ^3.12.0
resolution: workspace
dependencies:
  pub_semver: ^2.2.0
  yaml: ^3.1.3
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
