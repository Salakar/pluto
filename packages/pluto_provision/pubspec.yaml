name: pluto_provision
description: Host-buildable Pluto provisioning transactions and plutod protocol.
version: 0.1.0
publish_to: none
resolution: workspace
environment:
  sdk: ^3.12.0
dependencies:
  crypto: ^3.0.0
  pluto_manifest: ^0.1.0
dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.15
