name: pluto_validation_lab
description: Scripted, deterministic scenes for on-device e-ink renderer validation.
publish_to: none
version: 0.1.0
environment:
  sdk: ^3.12.0
resolution: workspace
dependencies:
  flutter:
    sdk: flutter
dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
