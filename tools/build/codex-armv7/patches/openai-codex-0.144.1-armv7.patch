diff --git a/codex-rs/Cargo.toml b/codex-rs/Cargo.toml
--- a/codex-rs/Cargo.toml
+++ b/codex-rs/Cargo.toml
@@ -555,6 +555,8 @@ opt-level = 0
 
 [patch.crates-io]
 # Uncomment to debug local changes.
+pagable = { path = "vendor/pagable" }
+seccompiler = { path = "vendor/seccompiler" }
 # ratatui = { path = "../../ratatui" }
 crossterm = { git = "https://github.com/nornagon/crossterm", rev = "87db8bfa6dc99427fd3b071681b07fc31c6ce995" }
 ratatui = { git = "https://github.com/nornagon/ratatui", rev = "9b2ad1298408c45918ee9f8241a6f95498cdbed2" }
diff --git a/codex-rs/code-mode/Cargo.toml b/codex-rs/code-mode/Cargo.toml
--- a/codex-rs/code-mode/Cargo.toml
+++ b/codex-rs/code-mode/Cargo.toml
@@ -18,12 +18,14 @@ workspace = true
 [dependencies]
 codex-code-mode-protocol = { workspace = true }
 codex-protocol = { workspace = true }
-deno_core_icudata = { workspace = true }
 futures = { workspace = true }
 serde_json = { workspace = true }
 tokio = { workspace = true, features = ["io-util", "macros", "process", "rt", "sync", "time"] }
 tokio-util = { workspace = true, features = ["rt"] }
 tracing = { workspace = true }
+
+[target.'cfg(not(all(target_arch = "arm", target_pointer_width = "32")))'.dependencies]
+deno_core_icudata = { workspace = true }
 v8 = { workspace = true }
 
 [dev-dependencies]
diff --git a/codex-rs/code-mode/src/lib.rs b/codex-rs/code-mode/src/lib.rs
--- a/codex-rs/code-mode/src/lib.rs
+++ b/codex-rs/code-mode/src/lib.rs
@@ -1,17 +1,41 @@
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 mod cell_actor;
 mod remote_session;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 mod runtime;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 mod service;
+#[cfg(all(target_arch = "arm", target_pointer_width = "32"))]
+mod service_armv7;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 mod session_runtime;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 mod v8_init;
+#[cfg(all(target_arch = "arm", target_pointer_width = "32"))]
+mod v8_init_armv7;
 
 pub(crate) type TaskFailureHandler = std::sync::Arc<dyn Fn(String) + Send + Sync>;
 
 pub use codex_code_mode_protocol::*;
 pub use remote_session::ProcessOwnedCodeModeSession;
 pub use remote_session::ProcessOwnedCodeModeSessionProvider;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 pub use service::InProcessCodeModeSession;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 pub use service::InProcessCodeModeSessionProvider;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 pub use service::NoopCodeModeSessionDelegate;
+#[cfg(all(target_arch = "arm", target_pointer_width = "32"))]
+pub use service_armv7::InProcessCodeModeSession;
+#[cfg(all(target_arch = "arm", target_pointer_width = "32"))]
+pub use service_armv7::InProcessCodeModeSessionProvider;
+#[cfg(all(target_arch = "arm", target_pointer_width = "32"))]
+pub use service_armv7::NoopCodeModeSessionDelegate;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 pub use v8_init::V8JitMode;
+#[cfg(not(all(target_arch = "arm", target_pointer_width = "32")))]
 pub use v8_init::initialize_v8;
+#[cfg(all(target_arch = "arm", target_pointer_width = "32"))]
+pub use v8_init_armv7::V8JitMode;
+#[cfg(all(target_arch = "arm", target_pointer_width = "32"))]
+pub use v8_init_armv7::initialize_v8;
diff --git a/codex-rs/code-mode/src/service_armv7.rs b/codex-rs/code-mode/src/service_armv7.rs
new file mode 100644
--- /dev/null
+++ b/codex-rs/code-mode/src/service_armv7.rs
@@ -0,0 +1,134 @@
+use std::sync::Arc;
+
+use codex_code_mode_protocol::CellId;
+use codex_code_mode_protocol::CodeModeNestedToolCall;
+use codex_code_mode_protocol::CodeModeSession;
+use codex_code_mode_protocol::CodeModeSessionDelegate;
+use codex_code_mode_protocol::CodeModeSessionProvider;
+use codex_code_mode_protocol::CodeModeSessionProviderFuture;
+use codex_code_mode_protocol::CodeModeSessionResultFuture;
+use codex_code_mode_protocol::ExecuteRequest;
+use codex_code_mode_protocol::ExecuteToPendingOutcome;
+use codex_code_mode_protocol::NotificationFuture;
+use codex_code_mode_protocol::StartedCell;
+use codex_code_mode_protocol::ToolInvocationFuture;
+use codex_code_mode_protocol::WaitOutcome;
+use codex_code_mode_protocol::WaitRequest;
+use codex_code_mode_protocol::WaitToPendingOutcome;
+use codex_code_mode_protocol::WaitToPendingRequest;
+use tokio_util::sync::CancellationToken;
+
+const UNSUPPORTED: &str = "code mode is unavailable on 32-bit ARM";
+
+pub struct NoopCodeModeSessionDelegate;
+
+impl CodeModeSessionDelegate for NoopCodeModeSessionDelegate {
+    fn invoke_tool<'a>(
+        &'a self,
+        _invocation: CodeModeNestedToolCall,
+        cancellation_token: CancellationToken,
+    ) -> ToolInvocationFuture<'a> {
+        Box::pin(async move {
+            cancellation_token.cancelled().await;
+            Err("code mode nested tools are unavailable".to_string())
+        })
+    }
+
+    fn notify<'a>(
+        &'a self,
+        _call_id: String,
+        _cell_id: CellId,
+        _text: String,
+        _cancellation_token: CancellationToken,
+    ) -> NotificationFuture<'a> {
+        Box::pin(async { Ok(()) })
+    }
+
+    fn cell_closed(&self, _cell_id: &CellId) {}
+}
+
+#[derive(Default)]
+pub struct InProcessCodeModeSessionProvider;
+
+impl CodeModeSessionProvider for InProcessCodeModeSessionProvider {
+    fn create_session<'a>(
+        &'a self,
+        _delegate: Arc<dyn CodeModeSessionDelegate>,
+    ) -> CodeModeSessionProviderFuture<'a> {
+        Box::pin(async {
+            let session: Arc<dyn CodeModeSession> = Arc::new(InProcessCodeModeSession);
+            Ok(session)
+        })
+    }
+}
+
+#[derive(Default)]
+pub struct InProcessCodeModeSession;
+
+impl InProcessCodeModeSession {
+    pub fn new() -> Self {
+        Self
+    }
+
+    pub fn with_delegate(_delegate: Arc<dyn CodeModeSessionDelegate>) -> Self {
+        Self
+    }
+
+    pub fn with_delegate_and_task_failure_handler(
+        _delegate: Arc<dyn CodeModeSessionDelegate>,
+        _task_failure_handler: crate::TaskFailureHandler,
+    ) -> Self {
+        Self
+    }
+
+    pub async fn execute(&self, _request: ExecuteRequest) -> Result<StartedCell, String> {
+        Err(UNSUPPORTED.to_string())
+    }
+
+    pub async fn execute_to_pending(
+        &self,
+        _request: ExecuteRequest,
+    ) -> Result<ExecuteToPendingOutcome, String> {
+        Err(UNSUPPORTED.to_string())
+    }
+
+    pub async fn wait(&self, _request: WaitRequest) -> Result<WaitOutcome, String> {
+        Err(UNSUPPORTED.to_string())
+    }
+
+    pub async fn terminate(&self, _cell_id: CellId) -> Result<WaitOutcome, String> {
+        Err(UNSUPPORTED.to_string())
+    }
+
+    pub async fn wait_to_pending(
+        &self,
+        _request: WaitToPendingRequest,
+    ) -> Result<WaitToPendingOutcome, String> {
+        Err(UNSUPPORTED.to_string())
+    }
+
+    pub async fn shutdown(&self) -> Result<(), String> {
+        Ok(())
+    }
+}
+
+impl CodeModeSession for InProcessCodeModeSession {
+    fn execute<'a>(
+        &'a self,
+        request: ExecuteRequest,
+    ) -> CodeModeSessionResultFuture<'a, StartedCell> {
+        Box::pin(InProcessCodeModeSession::execute(self, request))
+    }
+
+    fn wait<'a>(&'a self, request: WaitRequest) -> CodeModeSessionResultFuture<'a, WaitOutcome> {
+        Box::pin(InProcessCodeModeSession::wait(self, request))
+    }
+
+    fn terminate<'a>(&'a self, cell_id: CellId) -> CodeModeSessionResultFuture<'a, WaitOutcome> {
+        Box::pin(InProcessCodeModeSession::terminate(self, cell_id))
+    }
+
+    fn shutdown<'a>(&'a self) -> CodeModeSessionResultFuture<'a, ()> {
+        Box::pin(InProcessCodeModeSession::shutdown(self))
+    }
+}
diff --git a/codex-rs/code-mode/src/v8_init_armv7.rs b/codex-rs/code-mode/src/v8_init_armv7.rs
new file mode 100644
--- /dev/null
+++ b/codex-rs/code-mode/src/v8_init_armv7.rs
@@ -0,0 +1,17 @@
+/// Controls whether V8 may generate executable code at runtime.
+#[derive(Clone, Copy, Debug, Default, Eq, PartialEq)]
+pub enum V8JitMode {
+    #[default]
+    Enabled,
+    Disabled,
+}
+
+/// Code mode's V8 runtime is not built for the 32-bit ARM target.
+pub fn initialize_v8(_jit_mode: V8JitMode) -> Result<(), String> {
+    Err("code mode is unavailable on 32-bit ARM".to_string())
+}
+
+#[allow(dead_code)]
+pub(crate) fn ensure_v8_initialized() -> Result<(), String> {
+    initialize_v8(V8JitMode::Enabled)
+}
diff --git a/codex-rs/linux-sandbox/src/landlock.rs b/codex-rs/linux-sandbox/src/landlock.rs
--- a/codex-rs/linux-sandbox/src/landlock.rs
+++ b/codex-rs/linux-sandbox/src/landlock.rs
@@ -169,8 +169,8 @@ fn install_filesystem_landlock_rules_on_current_thread(
 fn install_network_seccomp_filter_on_current_thread(
     mode: NetworkSeccompMode,
 ) -> std::result::Result<(), SandboxErr> {
-    fn deny_syscall(rules: &mut BTreeMap<i64, Vec<SeccompRule>>, nr: i64) {
-        rules.insert(nr, vec![]); // empty rule vec = unconditional match
+    fn deny_syscall(rules: &mut BTreeMap<i64, Vec<SeccompRule>>, nr: libc::c_long) {
+        rules.insert(nr as i64, vec![]); // empty rule vec = unconditional match
     }
 
     // Build rule map.
@@ -212,8 +212,8 @@ fn install_network_seccomp_filter_on_current_thread(
                 libc::AF_UNIX as u64,
             )?])?;
 
-            rules.insert(libc::SYS_socket, vec![unix_only_rule.clone()]);
-            rules.insert(libc::SYS_socketpair, vec![unix_only_rule]);
+            rules.insert(libc::SYS_socket as i64, vec![unix_only_rule.clone()]);
+            rules.insert(libc::SYS_socketpair as i64, vec![unix_only_rule]);
         }
         NetworkSeccompMode::ProxyRouted => {
             // In proxy-routed mode we allow IP sockets in the isolated
@@ -242,8 +242,8 @@ fn install_network_seccomp_filter_on_current_thread(
                 SeccompCmpOp::Ne,
                 libc::AF_UNIX as u64,
             )?])?;
-            rules.insert(libc::SYS_socket, vec![deny_non_ip_socket]);
-            rules.insert(libc::SYS_socketpair, vec![deny_non_unix_socketpair]);
+            rules.insert(libc::SYS_socket as i64, vec![deny_non_ip_socket]);
+            rules.insert(libc::SYS_socketpair as i64, vec![deny_non_unix_socketpair]);
         }
     }
 
@@ -255,6 +258,8 @@ fn install_network_seccomp_filter_on_current_thread(
             TargetArch::x86_64
         } else if cfg!(target_arch = "aarch64") {
             TargetArch::aarch64
+        } else if cfg!(target_arch = "arm") {
+            TargetArch::arm
         } else {
             unimplemented!("unsupported architecture for seccomp filter");
         },
